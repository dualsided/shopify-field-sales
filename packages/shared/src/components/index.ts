/**
 * Shared Components
 *
 * Platform-agnostic component types and utilities.
 * Actual component implementations are in platform-specific packages.
 *
 * Usage:
 * - Import types from here for component interfaces
 * - Implement components in apps/field-app (web) or apps/mobile (React Native)
 */

export * from './types';
