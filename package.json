{
  "name": "shopify-field-sales",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "db:generate": "npm run db:generate --workspace=@field-sales/database",
    "db:push": "npm run db:push --workspace=@field-sales/database",
    "db:migrate": "npm run db:migrate --workspace=@field-sales/database",
    "db:seed": "npm run db:seed --workspace=@field-sales/database",
    "db:studio": "npm run db:studio --workspace=@field-sales/database"
  }
}
